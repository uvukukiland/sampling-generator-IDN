# Dependensi dengan batas atas (<major berikutnya) agar update besar yang
# berpotensi merusak TIDAK ikut terpasang otomatis -> app awet bertahun-tahun.
# Untuk reproducibility penuh (mis. arsip 10 tahun), pakai Docker + requirements-lock.txt.
streamlit>=1.43,<2     # width="stretch" butuh >=1.43
pandas>=2.2,<4
numpy>=1.26,<3
openpyxl>=3.1,<4
xlsxwriter>=3.1,<4
